# use-session.test.tsx

用可控 SSE 流和 renderHook 验证页面订阅生命周期。

- 快照恢复后合并增量，完成即停止订阅，卸载不取消服务端轮次：验证 done 替换与 reader 清理。
- 提交后的流意外结束只 GET 恢复，不重复 POST 或叠加快照：模拟网络 EOF 与重连退避。
- 取消提交准确的 session 和 turn，响应后解除运行状态：不用等待迟到 SSE 才解除按钮锁定。
- 旧会话的迟到响应不能覆盖新会话：路由切换后只接受当前连接。
