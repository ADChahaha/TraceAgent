# session-documents.test.tsx

- 显示整份文档，引用只高亮目标段落并滚动，不重新请求或替换上下文：验证完整开头、正文、列表及章节标题均显示；选中引用后只改变对应块的高亮并调用滚动，切换到同文档另一引用或刷新相同资源版本不重复请求全文。
- 按文档而不是片段切换，切换后加载另一整份文档：验证选择器按文档目录分组，跨文档引用载入新全文并移除旧文档内容。

- 默认只显示 Sources，阅读器替换整个列表，关闭后返回列表：验证默认不请求正文、打开引用时仍有完整上下文和唯一高亮，来源列表被替换，关闭回调后恢复列表。
