[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-service"
version = "0.1.0"
description = "Agent service for OCR preprocessing and document extraction."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "docling>=2.88.0",
    "fastapi>=0.115,<1",
    "langgraph>=0.2,<1",
    "langchain-openai>=0.3,<1",
    "openai>=2.28,<3",
    "python-docx>=1.1",
    "python-multipart>=0.0.9",
    "torch>=2.0",
    "uvicorn>=0.30,<1",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "Pillow>=10.0",
    "reportlab>=4.0",
]

[tool.setuptools]
py-modules = ["main"]
packages = [
    "service",
    "service.document_processor",
    "service.document_processor.impl",
    "service.document_processor.impl.docx",
    "service.document_processor.impl.pdf",
    "service.file_extraction_agent",
    "service.file_extraction_agent.impl",
    "service.route_policy_agent",
    "service.route_policy_agent.impl",
    "routes",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = ["--import-mode=importlib"]
