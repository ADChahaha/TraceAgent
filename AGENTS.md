# AGENT 规则

本文件适用于整个仓库及其子包。所有 `MUST` / `MUST NOT` 都是硬约束；更深目录中的规则优先。

## 1. 工作流

1. 确认目标服务、目录、目标文件和改动范围；信息不足时先问用户。
2. 修改前读取目标文件，以及最近适用的 `docs/DESIGN.md`。
3. 明确测试定义的目标行为。
4. 行为变更必须按 `red -> green -> refactor` 执行 TDD；纯文档、注释和无行为重命名除外。
5. 评估并同步必要的 `docs/DESIGN.md`、`DEVLOG.md` 和测试文档。
6. 完成代码、测试和文档同步并通过必要验证后，才能报告完成。

## 2. 必须遵守

- 只修改用户指定的文件；必要的测试、设计文档、开发日志和测试文档除外。
- `red` 阶段确认失败来自目标行为；`green` 阶段用最小实现通过；`refactor` 阶段保持行为不变。
- 模块边界、流程、职责或依赖变化时更新对应设计文档。
- 新增或修改测试时，在对应 `tests/docs/` 中维护一份同名对应的简洁说明，解释每个测试函数。
- 默认用中文写新增说明、注释和 docstring，标准英文术语除外。
- 解释模块时先写实现思路，再说明真实输入、校验、归一化或路由、实际调用、输出和关键异常。

## 3. 禁止事项

- 未读设计文档或目标文件前，不得编辑代码。
- 不得跳过 TDD、必要文档同步或测试文档同步。
- 不得未经批准修改无关文件，也不得用单文件偏好跳过必要覆盖。
- 不得先写实现后补测试，不得把实现完成当成任务完成。
- 不得写空泛的 pipeline、流水账文档或只列清单而不说明工作原理。

## 4. 提交规则

- 当当前任务的代码、测试、文档和验证都完成，且改动已达到可提交状态时，自动创建 commit，无需再次询问。
- 提交前检查 `git status` 和 `git diff`，只包含当前任务的改动，保留无关工作区修改。
- 每个 commit 只包含一个逻辑变更，提交信息简洁明确。

## 5. 输出、代码与格式

- 用户要求代码时先给代码；只有存在非显而易见内容时才解释。
- 不写行内 prose；注释只用于解释不清晰的逻辑，且应简洁。
- 使用最简单的可行方案，不做单次抽象、推测功能或不可能场景的错误处理。
- 修改前必须读文件；未读相关代码前不得推测 bug。
- 不为未修改的代码添加 docstring 或类型注解；三行相似代码优于过早抽象。
- 评审只说明 bug 和修复，不提范围外建议，不夸赞代码。
- 调试时说明发现的位置、原因和修复；原因不明就明确说明，不要猜测。
- 不使用 em dash、智能引号或装饰性 Unicode；使用普通连字符、直引号和可复制的代码。

## 6. 简要提醒

先读设计和文件，先写测试，再写实现；同步文档，验证结果，创建原子 commit。
